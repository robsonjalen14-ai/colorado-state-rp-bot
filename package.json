{
  "name": "charon-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers Discord interactions bot for Charon.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node src/commands.js",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.js && node --check src/worker.js && node --check src/discord.js && node --check src/github.js && node --check src/zip.js && node --check src/commands.js && node --check src/embeds.js && node --check src/tickets.js && node --check src/utils.js"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "wrangler": "^4.49.0"
  }
}
